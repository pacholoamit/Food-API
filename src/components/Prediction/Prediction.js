import React from 'react'


const Prediction = (onPrediction)  => {
	render() {
	return (


		<div>
		`Prediction: ${prediction.name}` {/*why is this not working? It should have referenced from parent, right?*/}
		 </div>
			);
		}
	}


export default Prediction;